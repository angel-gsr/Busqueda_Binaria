#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void main(void)
{
	int i,j,n,posAnterior=0,numActual=0;
	
	srand(time(NULL));
	printf();
	scanf();
	int lista[n];
	
	printf();
	for()
	{
		lista[i]=rand()%(100+1-1)+1;
		printf(" %d ",lista[i]);
	}
	printf("\n");
	
	for(i=0;i<n;i++)
	{
		numActual=lista[i];
		posAnterior=i-1;
		
		while(posAnterior>=0 && lista[posAnterior]>numActual)
		{
			
		}
	}
}
